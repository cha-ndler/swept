{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs the mac-cleaner window needs. The scan emits progress events, so the webview must be allowed to listen on the core event channel. No plugin permissions are granted: the app talks only to its own scan/login_items/clean commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
