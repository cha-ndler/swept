[package]
name = "swept-safety"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Trust kernel for Swept: protected-path denylist, canonicalization, scoped allowlist."

[lib]
name = "safety"
path = "src/lib.rs"

[dev-dependencies]
tempfile.workspace = true
proptest.workspace = true
