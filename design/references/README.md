# Competitor UX references

Drop screenshots of comparable tools here so the `ux-critic` can compare
mac-cleaner's layout, hierarchy, and affordances against the field. Suggested:

- `cleanmymac-*.png` — CleanMyMac (smart scan / system junk views)
- `daisydisk-*.png` — DaisyDisk (the sunburst size visualization + drag-to-collect)
- `onyx-*.png` — OnyX (maintenance/cleaning panes)
- `raycast-*.png`, `linear-*.png` — general polish exemplars (not cleaners)

## Important

These images are **added manually by a human** — do not auto-download them. They
are third-party, copyrighted UI; keep them out of any shipped artifact and use
them only as local design references for critique. This folder is intentionally
empty (except this README) until you add them; the `ux-critic` will fall back to
the rubric + platform conventions when no references are present.
