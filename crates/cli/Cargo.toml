[package]
name = "swept"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Safe, dry-run-first macOS junk cleaner (CLI front-end)."

[[bin]]
name = "swept"
path = "src/main.rs"

[dependencies]
swept-core = { path = "../core" }
swept-safety = { path = "../safety" }
clap.workspace = true
dirs.workspace = true

[dev-dependencies]
tempfile.workspace = true
