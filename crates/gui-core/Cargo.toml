[package]
name = "macclean-gui-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Frontend-facing command layer for the mac-cleaner GUI: thin, tested wrappers over macclean-core that return serializable DTOs."

[dependencies]
macclean-core = { path = "../core" }
macclean-safety = { path = "../safety" }
serde.workspace = true
dirs.workspace = true

[dev-dependencies]
tempfile.workspace = true
