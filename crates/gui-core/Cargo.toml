[package]
name = "swept-gui-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Frontend-facing command layer for the Swept GUI: thin, tested wrappers over swept-core that return serializable DTOs."

[dependencies]
swept-core = { path = "../core" }
swept-safety = { path = "../safety" }
serde.workspace = true
serde_json.workspace = true
dirs.workspace = true
# For atomically staging the acceptance record: a randomly named staging file
# closes the planted-symlink hole a predictable one opens. See acceptance.rs.
tempfile.workspace = true

[dev-dependencies]
